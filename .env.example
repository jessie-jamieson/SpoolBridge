# === SpoolEase Connection (REQUIRED) ===
BRIDGE_SPOOLEASE_HOST=192.168.1.50       # SpoolEase device IP or hostname
BRIDGE_SPOOLEASE_SECURITY_KEY=ABCDEFG    # 7-character security key from SpoolEase display
# BRIDGE_SPOOLEASE_PORT=80               # Default: 80
# BRIDGE_SPOOLEASE_USE_HTTPS=false       # Default: false

# === Spoolman Connection ===
# BRIDGE_SPOOLMAN_HOST=spoolman          # Default: spoolman (Docker service name)
# BRIDGE_SPOOLMAN_PORT=8000              # Default: 8000 (internal Docker port)

# === Sync Behavior ===
# BRIDGE_POLL_INTERVAL_SECONDS=30        # How often to poll SpoolEase (seconds)
# BRIDGE_DELTA_THRESHOLD=0.1             # Minimum grams change before syncing

# === Storage ===
# BRIDGE_MAPPING_FILE_PATH=/data/mapping.json

# === Logging ===
# BRIDGE_LOG_LEVEL=INFO                  # DEBUG, INFO, WARNING, ERROR
